# Flask Backend Basics

A simple Flask API built while learning backend development.

## Endpoints
- `/` → API running message
- `/health` → status check
- `/hello/<name>` → returns greeting
- `/info` → returns app info + date

## Run Locally
Server: http://127.0.0.1:5000/
